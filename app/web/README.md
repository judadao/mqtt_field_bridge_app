# Provisioning Web Source

The provisioning UI source is owned by `dephy_web`.

This product pins the MQTT field bridge web profile in `deps.json`:

```text
deps/dephy_web/products/mqtt_field_bridge/web/
```

The embedded header is generated from that dependency during builds.
